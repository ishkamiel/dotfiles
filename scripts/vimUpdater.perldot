#!/usr/bin/perl

run('mkdir -p ~/.vimbackup');

run('git clone https://github.com/gmarik/Vundle.vim.git /home/ishkamiel/.vim/bundle/Vundle.vim',
    {
        ignore_retval => 1
    });

run('vim +PluginUpdate +qall',
    {
        timed => [ 24, 0 ]
    });

