" SPDX-License-Identifier: MIT
" Copyright (C) 2026 Hans Liljestrand <hans@liljestrand.dev>
"
" Thin shim: real config lives in dot_config/nvim/init.vim. init.vim uses
" has('nvim') guards for neovim-only bits, so plain vim skips them cleanly.
if filereadable(expand('~/.config/nvim/init.vim'))
  source ~/.config/nvim/init.vim
endif

" vim: ft=vim shiftwidth=2 tabstop=2
